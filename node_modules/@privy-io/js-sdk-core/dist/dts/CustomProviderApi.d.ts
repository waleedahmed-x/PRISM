export default class CustomProviderApi {
    /**
     * Logs a user in via a custom JWT from another (non-Privy) service
     *
     * @param token The JWT from the non-Privy service
     */
    syncWithToken(token: string): Promise<import("@privy-io/public-api").PrivyAuthenticatedUser>;
}
